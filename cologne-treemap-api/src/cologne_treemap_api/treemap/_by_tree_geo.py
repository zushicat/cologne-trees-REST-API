import json
import datetime

from typing import Any, Dict, List

def get_tree_geo_name_german_suburb_numbers(
    treemap: List[Dict[str, Any]], suburb_number: str = None
) -> Dict[str, Any]:
    print(f"hello {suburb_number}")